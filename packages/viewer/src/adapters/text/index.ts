/* ============================================================
 * Text Adapter Entry — manifest + loader
 * ============================================================ */

export { textManifest, TextAdapter } from './TextAdapter';
export { TextAdapter as default } from './TextAdapter';
