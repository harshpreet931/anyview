/* ============================================================
 * Markdown Adapter Entry - manifest + loader
 * ============================================================ */

export { markdownManifest, MarkdownAdapter } from './MarkdownAdapter';
export { MarkdownAdapter as default } from './MarkdownAdapter';
